import { StyleSheet } from "react-native";
import { TWSizes } from "twrn-styles";

export const styles = StyleSheet.create({
    img: {
        width: TWSizes.percentageHeight(70),
        height: TWSizes.percentageHeight(40)
    }
})