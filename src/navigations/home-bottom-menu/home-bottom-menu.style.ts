import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    tabBarIndicatorStyle: {
        backgroundColor: '#5A91FF',
        top: -5,
    },
    fabContainer: {elevation: 10, bottom: 30}
})